# 实验原则（从主仓 PRINCIPLES 摘录与本仓相关条目）

1. **单变量原则**：本轮唯一变量 = 教师 rollout 评估器（手写冠军 → NN 66734.8）。
   采样空间、index 语义、ε、quota、分片方式全部不动。
2. **口径纪律**：教师评估器变了 → rollout 分数口径全变 → Python 侧 regret **只记不裁决**，
   与 008/009 的 regret 数字**不可直接比**。唯一裁决 = 闭环配对（EXP-010b，回主仓跑）。
3. **配对裁决判据**：t > 2 且 Δ > 0 才判超；基线=手写默认 65438.2、手写冠军 65554.2、
   gen3 最优 65669.2、gen4 最优 66734.8（四基线）。
4. **禁静默回退**：RAMEN_NN_MODEL 缺失/模型加载失败必须 panic，
   绝不允许教师悄悄退回手写（那会让本轮白跑且标签口径污染）。
5. **patch 单锚点断言**：所有 upstream patch 必须断言锚点恰好出现 1 次，防 pin 漂移。
6. **上游只读**：checkout pin d27a6ebd3e0e0daeadb1bd5a3d137b97747009d1，
   所有改动经 patch 脚本落到工作区，不向上游提 PR。
